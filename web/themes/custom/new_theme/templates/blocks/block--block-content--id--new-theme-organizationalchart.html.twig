<h2 class="text-2xl md:text-3xl font-bold  bg-white text-purple-800 text-left pl-10 mb-5">
    Organizational Chart
  </h2>
  <hr class="border-t-2 bg-white border-purple-800 mx-10 mb-10">
 {% if content.field_profile_image[0] is not empty %}
        {% set image_uri = content.field_profile_image[0]['#item'].entity.uri.value %}
        <section class="w-full  flex justify-center items-center bg-white px-10">
            <img src="{{ file_url(image_uri) }}" alt="Organizational Chart" class="max-h-full max-w-full object-contain">
        </section>
      {% endif %}